{% extends 'base.html.twig' %}
{% block body %}
<table border="1">
    <tr>
        <th>Nom</th>
        <th>role</th>
    </tr>
    {% for user in users %}
        <tr>
            <td>{{ user.username }}</td>
            <td>{{ user.roles[0] }}</td>
        </tr>
    {% endfor %}
</table>
{% endblock %}